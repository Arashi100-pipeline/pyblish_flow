[project]
name = "workflow-server"
version = "0.1.0"
description = "FastAPI SSE server for Vue Flow demo"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pyyaml",
  "rpyc>=6.0.2",
]

[tool.uv]
# can customize if needed

